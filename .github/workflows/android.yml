name: Get Firebase Keys

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  extract-keys:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Extract SHA-1
        run: |
          cd android
          chmod +x gradlew
          echo "************************************************"
          echo "🚀 COPIA IL CODICE SHA-1 QUI SOTTO:"
          echo "************************************************"
          ./gradlew signingReport | grep -A 3 "Variant: debug" || echo "Errore nella lettura delle chiavi"
          echo "************************************************"
